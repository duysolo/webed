sudo: true

php:
    - 5.6
    - 7.0
    - 7.1
    - 7.2
    - nightly

cache:
    directories:
    - $HOME/.composer/cache

services:
    - sqlite

before_script:
    - composer self-update
    - composer install --prefer-source --no-interaction --dev

script:
    #- vendor/bin/phpunit
